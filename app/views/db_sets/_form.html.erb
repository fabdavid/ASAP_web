<%= form_for(db_set) do |f| %>
  <% if db_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(db_set.errors.count, "error") %> prohibited this db_set from being saved:</h2>

      <ul>
      <% db_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
<tr>
<td>Database</td>
<% l =  Tool.all.select{|t| t.tool_type_id == 1}.map{|t| [t.label, t.id]} %>
<td><%= f.select :tool_id, l %></td>
</tr>
<tr>
<td>Label</td>
<td><%= f.text_field :label %></td>
</tr>
<tr>
<td>Tag</td>
<td><%= f.text_field :tag %></td>
</tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
