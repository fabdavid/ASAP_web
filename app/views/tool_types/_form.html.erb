<%= form_for(tool_type) do |f| %>
  <% if tool_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tool_type.errors.count, "error") %> prohibited this tool_type from being saved:</h2>

      <ul>
      <% tool_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
<tr><td>Name</td><td><%= f.text_field :name %></td></tr>
</table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
