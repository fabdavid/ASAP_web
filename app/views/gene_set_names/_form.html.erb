<%= form_for(gene_set_name) do |f| %>
  <% if gene_set_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gene_set_name.errors.count, "error") %> prohibited this gene_set_name from being saved:</h2>

      <ul>
      <% gene_set_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
