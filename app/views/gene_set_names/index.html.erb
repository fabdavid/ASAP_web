<p id="notice"><%= notice %></p>

<h1>Gene Set Names</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gene_set_names.each do |gene_set_name| %>
      <tr>
        <td><%= link_to 'Show', gene_set_name %></td>
        <td><%= link_to 'Edit', edit_gene_set_name_path(gene_set_name) %></td>
        <td><%= link_to 'Destroy', gene_set_name, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Gene Set Name', new_gene_set_name_path %>
